# 14. Состояние работы: где мы сейчас

Живой документ. Обновляется каждой сессией агента. Цель — чтобы следующий агент не
переоткрывал уже принятые решения и не искал точку входа заново.

Куда идём — `docs/12-target-state.md`. Как идём — `docs/13-roadmap.md`.

---

## Текущая точка

| | |
|---|---|
| Этап | **E2 в работе** (движок до полного контура), **E1 и E0 закрыты** |
| Закрыто в E2 | E2-01, E2-02 (полный банк), E2-03, E2-04, E2-05, E2-11 (доборы срезов) |
| Следующая задача | **E2-08 · Несогласие как данные** — зависимости закрыты; вместе с ней свободна E2-12 (сверка правил кода и контента) |
| Проверка состояния | `npm install && npm test` — 84 теста проходят; `npm run serve` открывает прототип |

## Закрыто с прошлой отметки

- E2-03 — таблицы вопросов в `content/slices/*.md` переразмечены в шесть колонок (идентификатор, текст, тип, координаты, варианты, назначение); сборщик отдаёт 95 вопросов по восьми срезам, сверяет их число с колонкой в `content/slices/README.md` и падает на свободно записанном варианте; формулировки не изменились ни в одном символе
- E2-04 — новый модуль `engine/src/slices.ts`: подтипы координат и флаги по разделам «Скоринг доборов», единый словарь подтипов, уверенность считает та же `scoreCoordinate`
- E2-05 — порог генерации каждого среза как машинное правило: не взят — отчёт не собирается, наружу уходят уточняющие вопросы из файла среза
- E2-11 — селектор следующей двери: таблица «Следующие двери» появилась у всех восьми срезов, предложение после среза всегда одно (`engine/src/offers.ts`)
- E2-01 — `content/questions-full-bank.md` переразмечен в таблицу из семи колонок (идентификатор, текст, тип, координаты, направление, роль, варианты); сборщик его разбирает и падает при любом расхождении формата; формулировки вопросов и вариантов не изменились ни в одном символе
- E2-02 — скоринг полного банка: все 16 координат считаются той же арифметикой, что и лестница; правила описаны в `content/scoring-rules.md`, раздел «Полный банк: точные правила»

Что именно сделано в E2-01: варианты ответа вынесены из ячейки с текстом вопроса в
отдельную колонку, направление и роль перестали быть пометками в скобках, идентификаторы
приведены к `Q1`–`Q35` и `О1`–`О3`. Тесты сверяют состав банка (35 закрытых и 3 открытых),
отсутствие следов разметки в формулировках, сборку строки таблицы обратно из разобранных
данных, а также обратные и ключевые вопросы и координаты — с `docs/02-coordinates.md` и с
лестницей.

Что именно сделано в E2-02: инверсия обратных вопросов, среднее, полоса, разброс и расчёт
confidence вынесены в одну функцию `scoreCoordinate`; ветка лестницы переведена на неё без
изменения своих правил и без правки своих тестов, ветка полного банка написана на ней же.
На ответах демо-человека из `examples/demo-person-answers.md` пустых координат не остаётся;
1, 6, 10, 12, 14 и 16 получают значения. Координаты 15 и 16 без синтеза открытых ответов
остаются пустыми — открытый текст движок не разбирает.

Что именно сделано в E2-03 — E2-11: словарь кодов подтипов, формулировки внутрь профиля,
пункты порога, уточняющие вопросы и таблицы дверей берутся из файлов срезов; в коде живут
только условия срабатывания, и их число сверяется с числом строк в контенте. Единый словарь
подтипов свёл разную точность одного положения: координата 13 (лестница — `needs_external_pull`,
банк — вариант `Q29`, добор — пусковой механизм) и координата 14 (`analysis` в банке против
`analytic` в срезе). Конфигурации пар S4 × S5 координате не принадлежат и живут отдельным
полем профиля. На подготовленном наборе доборов каждый из восьми срезов берёт порог, на
пустых ответах — не берёт и отдаёт свои уточняющие.

## Что дальше по маршруту

E2 продолжается E2-08 (несогласие как данные) и E2-12 (сверка правил кода и контента) —
без незакрытых зависимостей. E2-06 ждёт E5-04, E2-07 — E5-02.

Параллельно, не блокируя движок, можно брать контентные задачи E5-01 (реестр запрещённых
формулировок), E5-04 (промежуточные блоки прикладных срезов), E5-05 (подписи дверей под
профиль), E5-08 (юридические тексты — с местами для подстановки реквизитов).

Серверные и визуальные этапы (E3, E6) начинаются с решений по стеку — задачи E3-01 и E6-01.
Их обоснование записывается сюда, в раздел «Принятые решения».

---

## Принятые решения

Журнал. Не переоткрывать без явного запроса основателя. Первый блок — решения, которые
уже реализованы в репозитории; второй — решения по составу и порядку работы.

### Архитектура и код

| Решение | Обоснование |
|---------|-------------|
| Markdown — единственный источник правды; код его только читает | Интеллект продукта в текстах и правилах, а не в коде. Сборщик `engine/scripts/build-content.mjs` превращает `content/*.md` в типизированные данные, поэтому правка текста не требует правки кода |
| Условия срабатывания узлов и арифметика скоринга живут в коде, не в прозе | Прозой они однозначно не записываются. Компенсация: тесты сверяют состав и порядок узлов в коде с `content/step3-contradictions.md`, а состав полос карты — с таблицей в `docs/11-ui-page-spec.md` |
| Генерируемый `engine/src/generated/` не хранится в репозитории | Он производная от markdown; хранение создаёт второй источник правды. Пересборка входит в `npm test` и `npm run serve` |
| Ступени 1–3 — только lookup, без LLM | Данных мало, LLM на них льёт воду; нужна мгновенность и предсказуемость. Ступень 4 и платное — LLM |
| Координаты наружу не отдаются: клиент получает блоки, полосы, двери и предложение | Значения координат, `confidence` и машинные коды — внутренняя тайна продукта. Закреплено тестом: сериализованное состояние страницы не содержит `confidence` и кодов вида `at_80` |
| Карта показывает 7 полос из 16 координат | Полная карта — платный продукт; остальные девять координат видны как отсутствие. Это и есть закрытые двери |
| Лестница честно оставляет шесть координат пустыми (1, 6, 10, 12, 14, 16) | Не дефект, а устройство бесплатного входа. Тест фиксирует именно этот список, чтобы «улучшение» скоринга не заполнило их догадками |
| Дата рождения не участвует в скоринге вообще | Эшелон 3 не имеет права утверждать. Тест проверяет, что источники координат — только ответы лестницы |
| Сезон темы периода берётся по текущей дате, а не по дате рождения | Тема периода — про «сейчас», а не про человека. Дата рождения нужна для карточки и визуала |
| Тема периода не записывается в координату 16 | Иначе метафора просочится в утверждение. Живёт отдельным полем оформления |
| Один узел на ступени 3, остальные — закрытые двери | Один механизм читается, три размываются. Приоритет: расхождение самооценки и поведения → узел с уязвимостью → остальные |
| Узел не выдумывается: есть `NODE_NONE` | Если ни одна пара не сработала, честный текст сильнее подогнанного узла |
| Поведение важнее самооценки: значение берётся из поведенческого ответа, самооценка даёт флаг | Правило 2 из `docs/04-alignment-rules.md`. Расхождение — материал для раздела напряжения, а не шум |
| Цена показывается только у предложенной двери | Остальные платные двери без цен, иначе маршрут читается как витрина |
| `NODE_POTENTIAL_VS_DETAIL` ведёт сразу в прикладной срез за 1290 ₽ | Узлового среза под него нет, а срез «Работа и реализация» допускается первым предложением. Кандидат на проверку: при низкой конверсии в 1290 ₽ первым чеком написать под узел срез за 590 ₽ |
| Открытый ответ короче 15 слов не даёт ни блока, ни предложения | Синтез на двух словах — вода. Порог проверяется тестом |
| Числа вопросов, цены и темы в текстах предложений — подстановки, а не литералы | Источник — таблица в `content/slices/README.md`, движок отдаёт их полями оффера (`engine/src/offers.ts`). Литерал в шаблоне разъезжается с контентом при первой правке добора |
| Прототип без сборщика: ванильный ES-модуль плюс статический сервер без зависимостей | Проверялся сценарий, а не стек. Прототип не является продакшен-фронтом и будет заменён в E7 |
| Состояние прототипа лежит в `localStorage` | Временное решение до появления сервера (E3). В целевом состоянии локально хранится только черновик текущего ответа |
| TypeScript в строгом режиме, тесты на `node:test`, без внешних фреймворков | Две зависимости на весь проект; движок обязан оставаться переносимым |
| Направление вопроса в банке задаётся относительно группы вопросов его основной координаты, а не относительно порядка полюсов в `docs/02-coordinates.md` | Порядок полюсов в `docs/02` со знаком координаты совпадает не везде (4, 5, 11), а внутри группы вопросов пометки «прямой/обратный» согласованы. Арифметике нужна именно согласованность внутри группы; какой полюс считать верхним — решает код по координате |
| Полоса координаты растёт в сторону второго полюса из `docs/02-coordinates.md`; исключение — координата 5 | У координаты 5 полоса растёт к «определённости», это уже заложено в правилах лестницы, а карта разворачивает её при показе (`engine/src/map.ts`). Переворачивать координату ради красоты — переписывать правила лестницы |
| Контрольного списка текстов вопросов в коде нет: неизменность формулировок проверяется сборкой строки таблицы обратно и отсутствием следов разметки | Список текстов в тесте — второй источник правды; при калибровке банка (E5-11) он потребовал бы правки кода на каждую правку текста |
| `Q26` помечен обратным относительно группы координаты 11, хотя в `docs/02-coordinates.md` пометки у него нет | В группе координаты 11 прямой полюс задаёт `Q23` («довожу»), а `Q26` («не сдвинусь без внешнего срока») смотрит в обратную сторону. `docs/02` — документ архитектуры, правим его только с ответом основателя (открытый вопрос 13) |
| Роль `ключевой` у `Q34` записана в банк | В `docs/02-coordinates.md` она указана, в банке её не было. Тест теперь сверяет роли банка с `docs/02` |
| Арифметика скоринга — одна функция `scoreCoordinate`, обе ветки входа обязаны ходить в неё | Два входа в один скоринг иначе расходятся в два набора правил. Потолки confidence лестницы вынесены в отдельную таблицу по контенту и накладываются поверх общего расчёта |
| Координаты 15 и 16 заполняются только структурированным результатом синтеза открытых ответов | Движок текст не интерпретирует: это работа LLM (E4-07). Без синтеза координаты честно остаются пустыми — как и в лестнице |
| Координата 14 считается голосами категорий, а не средним | «Способ решать» — не шкала, а четыре режима (анализ, отклик, обсуждение, пауза); усреднять их бессмысленно. Голос — ответ в верхней полосе или названный вариант; ответ за другую категорию считается спорящим |
| Код координаты 13 у лестницы и у банка разной точности (`needs_external_pull` против `invited` / `on_request` / `many_tries`) | В лестнице ключевого вопроса координаты нет, есть только косвенный `L11`; выдумывать точность нельзя. Сведено в E2-04 единым словарём подтипов: точный код уточняет грубый, и это согласие, а не спор |
| Единый словарь подтипов живёт в `engine/src/slices.ts`: коды и формулировки — из контента, связи «что уточняет что» — в коде | Формулировки правит основатель, связи прозой не записываются. Тест сверяет состав кодов кода с текстом файла среза и требует, чтобы уточнение указывало на код той же координаты |
| Варианты доборов вынесены в отдельную колонку, `как в S4` — ссылка на варианты другого вопроса | Тот же приём, что в банке. Ссылка нужна ровно одному вопросу (`slice_work` S5), у которого в тексте прямо написано «варианты те же A–E» |
| Вопрос типа «число» с двумя величинами приходит массивом из двух чисел | Три вопроса спрашивают два числа сразу («начинал / дошло», «часов всего / из них своё», «могут позвонить мне / могу позвонить я»). Разбор строки движком — угадывание |
| У вопроса-добора координата может быть пустой | `slice_relationships` S10 — оценка справедливости претензии из S9; он работает в паре, а не питает координату |
| Прежнее чтение координаты при уточнении считается согласным ответом, только если словарь связывает коды; иначе оно молчит, а не спорит | Иначе любое уточнение выглядело бы как расхождение и ставило ложный флаг. Настоящие расхождения у каждого среза записаны своим условием в его файле |
| Конфигурации срезов (пары S4 × S5, типы зависания развилки) не принадлежат координате и живут полем `configurations` профиля | В контенте у них нет координаты, и приписывать её нельзя. Отчёт читает их как находки среза |
| Структурная часть кризисной проверки среза работает в движке и останавливает отчёт без уточняющих; лексическая — E2-07 | `S1=F` в `slice_stress` — это состояние, а не профиль. Уточняющие вопросы в этом месте были бы продолжением разбора вместо помощи |
| Порог `slice_decision_moment` требует внешнего вердикта о безопасности темы | Пункт «развилка не относится к медицине, психиатрии, юридическому риску или безопасности» жёсткий, а решается разбором текста. Движок его не угадывает: без вердикта порог не взят |
| Флаг `fork_ahead` дописан в `slice_work` машинным идентификатором | Правило «`slice_decision_moment`, если S20 про развилку» иначе не выразить: S20 открытый, а флаг обязан быть назван в контенте, иначе тест его не пропустит |
| Таблица «Следующие двери» появилась у всех восьми срезов, последняя строка — «иначе» | Правила уже были записаны прозой в тех же файлах («один пункт ведёт в `slice_work`»); в таблице они машинно-читаемы. Строка «иначе» гарантирует, что предложение всегда одно |
| Общее правило «середина полосы и противоречия → low» применено в общей арифметике, включая ветку лестницы | Иначе появляется второй набор правил. На непокрытых входах лестницы это меняет medium на low в нескольких сочетаниях; на продукт не влияет — карта различает только `high`, остальное показывается как «предположение» |
| Расхождение с ожиданиями в `examples/demo-person-answers.md` по координатам 8 и 9 (medium вместо high) не правится | Уязвимость демо-человека подтверждается его открытыми ответами, а подтверждение текстом считает только LLM; по координате 9 `Q21` спорит с ключевым `Q20`, и это честный medium. Файл — заметка к демо, а не приёмка |

### Состав работы и порядок

| Решение | Обоснование |
|---------|-------------|
| Живые люди — последний этап маршрута (E12), а не первый | Прямое указание основателя. Реакция на черновик измеряет черновик; калибровать тексты дважды — потерянная работа |
| Техническая и визуальная части закрываются полностью до E12 | Виральность страницы — единственный канал роста, который у продукта есть; проверять его на неготовом визуале бессмысленно |
| В цель первого релиза входят восемь срезов с написанными доборами плюс полная карта | Под них есть вопросы, скоринг, пороги и промпт-надстройки; они закрывают маршрут от 590 ₽ до 1990 ₽ |
| Подписка, срез периода и срез совместимости — вне цели первого релиза | Подписке и срезу периода нужна накопленная история и регулярные платежи; совместимости — поток второго человека целиком. Двери на карте остаются, генерации нет |
| Мобильные приложения, B2B white-label, другие языки, детские отчёты — вне цели | Границы в `docs/12-target-state.md`. Ни одно из них не усиливает первый релиз, каждое тянет отдельный контур |
| Микрокопия выносится в контент как реестр строк с идентификаторами | Иначе тексты расползаются по коду и выпадают из линтера запрещённых формулировок |
| Кризисный детектор — код, кризисные тексты и контакты — контент | Триггеры прозой не записываются, а тексты и контакты обязаны правиться без деплоя |
| Пороги генерации срезов заданы строго и ожидаемо поедут вниз | Лучше не выдать отчёт, чем выдать пустой. Калибровка — E12-05, после живых ответов |

---

## Открытые вопросы к основателю

Решить самостоятельно нельзя: либо ломает продуктовые решения, либо требует данных,
которых в репозитории нет. Пока вопрос открыт, зависимые задачи либо не берутся, либо
делаются с явными местами для подстановки.

| # | Вопрос | Что блокирует | Предложение агента |
|---|--------|---------------|--------------------|
| 1 | Цены фиксируются окончательно? В `docs/07-monetization-route.md` диапазоны (490–790, 990–1490, 1490–2490), в `content/slices/` конкретные 590 / 1290 / 1490 / 1990 | E8-08, тексты оплаты E5-09 | Оставить как в `content/slices/`, диапазоны в `docs/07` считать историей решения |
| 2 | Юридическое лицо и налоговый режим | E5-08 (оферта, политика), E8-01, чеки и 54-ФЗ | До ответа писать юридические тексты с местами для подстановки реквизитов |
| 3 | Платёжный провайдер | E8-01 и весь E8 | Выбор зависит от ответа на вопрос 2 |
| 4 | Домен и имя продукта в интерфейсе и на шеринговой картинке | E10-01, E6-11, E5-10 | Без имени продукта шеринговая картинка не собирается |
| 5 | LLM-провайдер и допустимый предел себестоимости одного профиля | E4-01, E4-10 | Предел нужен как число: от него зависят лимиты повторных генераций |
| 6 | Собираем ли электронную почту | E5-07 (письма), E8-05 (чек), сценарий возврата | Ссылка на страницу как основной носитель, почта — только для чека и готовности среза; иначе вход перестаёт быть «20 секунд» |
| 7 | Список контактов помощи, которые публикуем в кризисном тексте | E5-02, E9-06 | Требуется решение основателя: публикуем государственные линии, конкретные организации или и то и другое |
| 8 | Шифруем открытые ответы при хранении или фиксируем отказ | E9-08, E3-03 | Шифровать открытые ответы и тексты блоков; решение зафиксировать здесь |
| 9 | Инструмент аналитики: свой сбор событий или внешний сервис | E10-07 | Свой сбор: внешний сервис добавляет согласие на передачу данных третьим лицам |
| 10 | Входит ли срез совместимости в первый релиз | Состав E4-08, E7-11 | Отложить: нужен полный поток второго человека. Дверь на карте оставить без цены |
| 11 | Приёмку визуала подписывает основатель по витрине компонентов или достаточно автотестов и снимков | E11-08 | Витрина компонентов плюс визуальные регрессии; ручной взгляд основателя на состояния `s0`–`paid_done` перед E12 |
| 12 | Одно имя «бесплатный финал» носят два разных отчёта: блок ступени 4 (250–350 слов) и полный бесплатный разбор без лестницы (900–1200 слов) | E4-02, E4-04 (валидатор объёма), E5-12 | Развести по именам: **«финал лестницы»** (тип `финал_лестницы`) и **«полный бесплатный разбор»** (тип `бесплатный_полный`). Переименование трогает `docs/06`, `prompts/full-report-assembler.md`, `content/step4-open-synthesis.md` — сделано не будет без ответа |
| 13 | В `docs/02-coordinates.md`, раздел «Пять сшивок», номера читаются двояко: как номера вопросов или как номера координат | E5-11 | Считать, что там номера **координат**, а `Q34`/`Q29` — ключевые вопросы этих координат, и переписать раздел однозначной записью: «координата N (вопросы …)». Разбор ниже |
| 14 | Как человек попадает на полный банк 35+3 в продукте? Скоринг банка есть, входа нет: лестница даёт 11+1, платные срезы — доборы по теме | E4-04 (объём отчёта), E5-12, состав первого релиза | Считать полный банк входом «полного бесплатного разбора без лестницы» из вопроса 12 и ручного прогона; порций и подводок под него не писать, пока основатель не подтвердит, что этот вход в первом релизе есть |
| 15 | Координата 14 («способ решать») опирается на два своих вопроса (`Q32`, `Q33`) и два чужих (`Q2`, `Q11`), а категория «анализ» держится на одном варианте `Q11` | E5-11, точность координаты 14 | Дописать в E5-11 калибровку координаты 14: свой вопрос на «анализ» и свой на «обсуждение». До этого координата 14 честно не поднимается выше medium |
| 16 | Таблицы подтипов срезов покрывают не все сочетания ответов: `slice_node_finish` S1=A + S2=D, `slice_reactivity` S8=C + S3=A/B, `slice_decisions` S2=A + S3=D и ещё несколько | Порог этих срезов, полнота отчёта | Не угадывать: без подтипа порог не берётся и человек получает уточняющие. Дописать недостающие строки в E5-11 |
| 17 | `slice_decisions` требует координату 5 не ниже medium, а его собственный триггер `NODE_PLAN_VS_OPEN` по правилам лестницы даёт этой координате ровно `low` | Порог `slice_decisions` | Уточнять координату 5 поведением из S1 добора (закрыл решение сам или дал закрыться) — сделано; правило порога и правило лестницы всё равно спорят, и это стоит развести словами в контенте |
| 18 | У `slice_relationships` и `slice_decision_moment` в файле нет ни одного правила следующей двери | E2-11, маршрут после прикладных срезов | Пока таблица состоит из одной строки «иначе» → `slice_full_map`. Нужны условия основателя: после близости и после развилки логичная дверь не очевидна |

### К вопросу 12: почему это две сущности

`docs/06-report-structure.md` в таблице объёма даёт «Финал бесплатного (LLM) — 250–350
слов». Столько же и в `content/step4-open-synthesis.md`: три абзаца, сюжет — механизм круга
— обрыв, каркас из семи частей туда физически не помещается. `prompts/full-report-assembler.md`
объявлен «для полного бесплатного (если без лестницы)» и задаёт `бесплатный_финал:
900–1200 слов` по тому же каркасу из семи частей с пропуском пятой. Это разные продукты:
блок ступени 4 внутри лестницы и полный разбор для сценария без лестницы. Путаница живёт
и внутри `docs/06`: первая строка документа и пункт про «без решения в п.5» описывают
второй отчёт, а таблица объёма — первый.

### К вопросу 13: разбор номеров в «Пяти сшивках»

Сейчас записано: «1. Тревога с адресом: Q7 + Q8/Q34», «2. Дисциплина и вход: Q11 + Q13/Q29».

**Гипотеза А — имелись в виду номера вопросов.** Тогда в сшивке 1 «насколько» — это
координата 7 (эмоциональная реактивность), её вопросы Q17, Q18 и обратный Q19, а «из-за
чего» — координата 8, её вопросы Q34 (ключевой) и Q21; верная запись: «Q17/Q18 + Q34/Q21».
В сшивке 2 «дисциплина» — координата 11 (Q23, Q24 ключевой, Q25, Q26), «вход» — координата
13 (Q29 ключевой, Q30, Q31); верная запись: «Q24 + Q29». Из пяти текущих номеров при этой
гипотезе верен только Q29, остальные четыре мимо: Q7 — «раздражают рассуждения без
конкретики» (координата 3), Q8 — «новая
возможность» (координаты 3, 4), Q11 — «две работы» (координаты 4, 14), Q13 — «оставляю
решение открытым» (координата 5).

**Гипотеза Б — имелись в виду номера координат.** Тогда сшивка 1 — координата 7
(«насколько») плюс координата 8 («из-за чего») с её ключевым вопросом Q34, сшивка 2 —
координата 11 (дисциплина) плюс координата 13 (вход) с её ключевым вопросом Q29. Ни одного
несовпадения. Гипотезу подтверждают остальные три сшивки: «Как учится и решает: Q3 + Q4 +
Q6» читается как координаты 3 (тип внимания), 4 (основание решений), 6 (открытость новому),
а как вопросы — Q3 и Q4 про темп, Q6 про внимание, и к «учится и решает» отношения не
имеют; «Где ломается в отношениях: Q12 + Q8» — координаты 12 (позиция среди людей) и 8
(базовая уязвимость), а вопросы Q12 и Q8 про план и про новую возможность; пятая сшивка
прямо написана словом «координаты».

E2-02 закрыта без опоры на этот раздел: сшивки — интерпретационный слой отчёта, а не
арифметика координат, и скоринг полного банка их не считает. Вопрос остаётся открытым для
E5-11 и для текстов, которые сшивки используют.

**Предложение.** Гипотеза Б, запись двусмысленная. Переписать раздел так, чтобы префикс `Q`
означал только вопрос: 1 — координата 7 (Q17, Q18) + координата 8 (Q34, Q21); 2 —
координата 11 (Q24) + координата 13 (Q29); 3 — координаты 3, 4, 6; 4 — координата 12 (Q27,
Q28) + координата 8 (Q34) + привязанность; 5 — О1–О2 + координаты 9, 11, 15. Смысл сшивок
при этом не меняется, но `docs/02-coordinates.md` — документ архитектуры, и правка ждёт
подтверждения основателя.

---

## Как обновлять этот файл

Закончив задачу: поменять её статус в `docs/13-roadmap.md`, перенести её в раздел «Закрыто
с прошлой отметки» здесь и указать следующую задачу в «Текущей точке». Любое решение,
которое пришлось принять по ходу, дописать в «Принятые решения» одной строкой с
обоснованием; любой вопрос, который не удалось решить, не ломая архитектуру, — в «Открытые
вопросы к основателю».
