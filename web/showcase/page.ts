/**
 * Сборка страницы живёт в `web/src/page.ts`. Витрина и живой клиент
 * импортируют одну функцию: двух реализаций нет.
 */

export {
  currentQuestion,
  filledBars,
  renderPersonalPage,
  type PageNotice,
  type PageViewLabels,
  type PageViewOptions,
} from "../src/page.js";
