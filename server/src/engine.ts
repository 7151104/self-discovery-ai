/**
 * Единственная точка, где сервер видит движок.
 *
 * Движок — единственная реализация правил: скоринг, узлы, блоки, карта и офферы
 * считаются только там. Сервер добавляет транспорт и хранение и ничего из этого
 * не повторяет. Файлы `engine/` сервер не меняет.
 */

export * from "../../engine/dist/index.js";
