/**
 * Печатает новый ключ шифрования: `npm run keygen`.
 *
 * Ключ печатается в поток вывода и никуда не записывается — ни в файл, ни в
 * базу. Положить его в `.env` или в хранилище секретов окружения должен человек.
 */

import { generateKey } from "./crypto.js";

process.stdout.write(`${generateKey()}\n`);
